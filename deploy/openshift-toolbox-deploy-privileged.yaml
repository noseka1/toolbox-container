apiVersion: apps/v1
kind: Deployment
metadata:
  name: openshift-toolbox
spec:
  template:
    spec:
      containers:
      - name: openshift-toolbox
        securityContext:
          privileged: true
